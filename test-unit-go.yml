test:unit:go:
  stage: code_quality
  image: "${CI_REGISTRY_IMAGE}/${APP_TEST_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"

  script:
    - go test ./...

  only:
    - /^v.+$/i
    - master
    - merge_requests

  except:
    variables:
      - $SKIP_CI_TEST == "true"
      - $CI_FAST_TRACK == "true"
