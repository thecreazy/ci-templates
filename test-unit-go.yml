test:unit:go:
  stage: code_quality
  image: "${APP_TEST_IMAGE}"

  script:
    - go test ./...

  only:
    - /^v.+$/i
    - master
    - merge_requests

  except:
    variables:
      - $SKIP_CI_TEST == "true"
      - $CI_FAST_TRACK == "true"
